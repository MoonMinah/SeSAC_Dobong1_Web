<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>form 배우기</title>
  </head>
  <body>
    <h2>GET 요청</h2>
    <!--form태그안에 method는 대문자,소문자 상관없다-->
    <form action="/getForm" method="GET">
      <label>
        id:
        <input type="text" name="id" />
      </label>
      <br />
      <label>
        email:
        <input type="email" name="email" required />
      </label>
      <br />
      <label>
        password:
        <input type="password" name="password" />
      </label>
      <br />

      <button type="button">제출(button type)</button>
      <button>제출(submit type)</button>
    </form>

    <h2>POST 요청</h2>
    <form action="/postForm" method="post">
      <label for="id">ID</label>
      <input
        type="text"
        id="id"
        name="id"
        pattern="^([a-zA-Z가-힣]){4,10}$"
        title="아이디는 영어대소문자, 숫자, 한글로 이루어진 4자 이상 10자 이하입니다."
        placeholder="id를 입력해주세요"
        required
      />
      <br />
      <label for="pw">비밀번호</label>
      <input
        type="password"
        id="pw"
        name="pw"
        pattern="^([a-z0-9]{8,12})$"
        placeholder="비밀번호를 입력해주세요"
        required
      />

      <fieldset>
        <legend>체크박스</legend>
        <!--checkbox에서는 value값을 써줘야 어떤걸 체크했느지 뜬다 안써주면 on 표시-->
        <label>
          <input type="checkbox" name="agree" value="마케팅 동의" />마케팅 동의
        </label>
        <br />
        <label>
          <input
            type="checkbox"
            name="agree"
            value="개인정보 수집 동의"
          />개인정보 수집 동의
        </label>
        <br />
        <label>
          <input type="checkbox" name="agree" value="이용약관 동의" />이용약관
          동의
        </label>
      </fieldset>

      <input type="submit" value="제출" />
    </form>
    <h2>Validation</h2>
    <form
      action="/js-form-check"
      method="post"
      name="validationForm"
      target="_blank"
      onsubmit="return checkForm(this)"
    >
      <!--
        checkbox의 required를 처리하기위해 onsubmit을 써준다.
        위의 this는 onsubmit 체크폼 자체를 의미한다
        체크폼이라는건 submit이 됐을 때 발동한다.
    -->
      <input
        type="text"
        name="id"
        minlength="1"
        placeholder="ID를 입력해주세요"
        title="최소 한글자 이상을 입력해주세요"
        id="input"
      />
      <br />
      <div>우편물 수령 장소</div>
      <!-- radio일 때는 required를 하나에만 넣어도 되지만 checbox는 아니다-->
      <label>
        <input type="checkbox" name="mail" value="회사" required />회사
      </label>
      <label>
        <input type="checkbox" name="mail" value="자택" required />자택
      </label>

      <button>제출</button>
    </form>
    <hr />
    <div>
      <a href="/practice1">실습문제1(GET)</a>
      <br />
      <a href="/practice2">실습문제2(POST)</a>
    </div>

    <script>
      //this로 자기자신을 담아 보냈으므로 변수에 form으로 써준다
      /*타겟을 self로 하면 페이지 전환이 일어나 보고싶은 정보를 못보지만
        form타겟을 _black로 써줬으므로 form이 제출되어도 페이지는 
        그래도 있고 다른 페이지로 응답페이지가 열린다.
      */
      function checkForm(form) {
        console.log(form);
        //const form = document.querySelector('.form')
        //const form = document.forms["validationForm"]
        //const form = document.forms["네임속성"]
        console.log(form.input); //id로 가져오기
        console.log(form.id); //name으로 가져오기

        /* input text 창에 아무것도 쓰지 않았을 때 */
        if (form.id.value === "") {
          alert("아이디를 입력해 주세요");

          //submit이벤트를 실행하지 않도록 return false적용
          return false;
        }

        /* 체크박스에서 아무것도 선택되지 않았을 때 */
        if (form.mail[0].checked === false && form.mail[1].checked === false) {
          alert("우편물 수령장소를 선택해주세요");
          return false;
        }
      }
    </script>
  </body>
</html>
